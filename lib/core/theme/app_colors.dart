import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color brandPrimary = Color(0xFF6750A4);

  // Status Colors - Light
  static const Color successLight = Color(0xFF2E7D32);
  static const Color errorLight = Color(0xFFD32F2F);

  // Status Colors - Dark
  static const Color successDark = Color(0xFF81C784);
  static const Color errorDark = Color(0xFFE57373);

  // Deep Space Gradient (Dark Theme Card)
  static const List<Color> deepSpaceGradient = [Color(0xFF0F0C29), Color(0xFF302B63), Color(0xFF24243E)];

  // Neutrals & Utility
  static const Color glassWhite = Color(0x1AFFFFFF);
  static const Color glassBorder = Color(0x1AFFFFFF);
  static const Color shadowDark = Color(0x33000000);
}
